<table {% for attribute_name,attribute_value in table.attributes %}{{ attribute_name }}="{{ attribute_value }}"{% endfor %}>
<thead>
<tr>
    {% for column in table.headers %}
        <th>{{ column }}</th>
    {% endfor %}
</tr>
</thead>
<tbody>
{% for row in table.rows %}
    <tr>
        {% for column in row %}
        <td>{{ column }}</td>
        {% endfor %}
    </tr>
{% endfor %}
</tbody>
</table>

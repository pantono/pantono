{% extends 'admin/layout.twig' %}
{% block content %}
    <div class="row">
        <div class="page-header">
            <h1>{{ app.translator.trans('Add Category') }}</h1>
        </div>
    </div>

    <div class="row">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}